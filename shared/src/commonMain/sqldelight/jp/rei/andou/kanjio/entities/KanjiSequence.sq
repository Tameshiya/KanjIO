CREATE TABLE IF NOT EXISTS kanji_sequence(
	code INTEGER NOT NULL,
    level_id INTEGER NOT NULL,
	sequence INTEGER NOT NULL,
	PRIMARY KEY (code, level_id),
	FOREIGN KEY (code) REFERENCES kanji(code) ON DELETE CASCADE,
    FOREIGN KEY (level_id) REFERENCES groups_levels(id) ON DELETE CASCADE
);